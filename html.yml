<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Modern Static Website</title>
  <meta name="description" content="A clean, responsive static website template." />
  <style>
    :root{
      --bg:#0f1724; /* deep navy */
      --card:#0b1220;
      --muted:#9aa8bf;
      --accent:#6ee7b7;
      --glass: rgba(255,255,255,0.04);
      --maxw:1100px;
      --radius:14px;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      background: linear-gradient(180deg, #071024 0%, #081427 60%);
      color:#e6eef8;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.5;
      padding:32px 16px;
      display:flex;justify-content:center;
    }
    .container{width:100%;max-width:var(--maxw)}

    /* Header / Nav */
    header{display:flex;align-items:center;justify-content:space-between;margin-bottom:28px}
    .brand{display:flex;gap:12px;align-items:center}
    .logo{width:44px;height:44px;border-radius:10px;background:linear-gradient(135deg,var(--accent),#60a5fa);display:flex;align-items:center;justify-content:center;font-weight:700;color:#053;box-shadow:0 6px 18px rgba(7,11,20,0.6)}
    nav{display:flex;gap:18px;align-items:center}
    nav a{color:var(--muted);text-decoration:none;font-weight:600}
    nav a.cta{background:var(--accent);color:#012; padding:8px 12px;border-radius:10px;text-decoration:none}

    /* Hero */
    .hero{display:grid;grid-template-columns:1fr 420px;gap:28px;align-items:center;margin-bottom:36px}
    .hero .left{padding:28px;background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent);border-radius:var(--radius);box-shadow:0 8px 30px rgba(2,6,23,0.6)}
    h1{font-size:28px;margin:0 0 12px}
    p.lead{color:var(--muted);margin:0 0 18px}
    .cta-row{display:flex;gap:12px}
    .btn{padding:10px 14px;border-radius:12px;background:transparent;border:1px solid rgba(255,255,255,0.06);cursor:pointer;color:inherit;font-weight:700}
    .btn.primary{background:var(--accent);color:#031018;border:none}

    /* Card on right */
    .mock{background:linear-gradient(180deg,#061226,#07182b);border-radius:16px;padding:18px;border:1px solid rgba(255,255,255,0.03)}
    .mock .row{display:flex;gap:12px;align-items:center}
    .avatar{width:48px;height:48px;border-radius:10px;background:linear-gradient(135deg,#e6f7ee,#dbeafe);display:flex;align-items:center;justify-content:center;color:#043;font-weight:700}

    /* Features */
    .features{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:36px}
    .feature{background:var(--card);border-radius:12px;padding:18px;border:1px solid rgba(255,255,255,0.03)}
    .feature h3{margin:0 0 6px}
    .feature p{color:var(--muted);margin:0}

    /* Gallery */
    .gallery{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:36px}
    .tile{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));height:110px;border-radius:10px;display:flex;align-items:center;justify-content:center;color:var(--muted);font-weight:700}

    /* Contact */
    .contact{display:grid;grid-template-columns:1fr 360px;gap:20px;margin-bottom:36px}
    form{background:var(--card);padding:18px;border-radius:12px;border:1px solid rgba(255,255,255,0.03)}
    label{display:block;font-size:13px;margin-bottom:6px;color:var(--muted)}
    input[type="text"],input[type="email"],textarea{width:100%;padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}
    textarea{min-height:120px}

    /* Footer */
    footer{display:flex;justify-content:space-between;color:var(--muted);font-size:14px;padding:18px;border-radius:12px}

    /* Responsiveness */
    @media (max-width:980px){
      .hero{grid-template-columns:1fr}
      .contact{grid-template-columns:1fr}
      .features{grid-template-columns:repeat(2,1fr)}
      .gallery{grid-template-columns:repeat(3,1fr)}
    }
    @media (max-width:640px){
      nav{display:none}
      .features{grid-template-columns:1fr}
      .gallery{grid-template-columns:repeat(2,1fr)}
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="brand">
        <div class="logo">MS</div>
        <div>
          <div style="font-weight:800">ModernSite</div>
          <div style="font-size:12px;color:var(--muted)">A clean static template</div>
        </div>
      </div>
      <nav>
        <a href="#features">Features</a>
        <a href="#gallery">Gallery</a>
        <a href="#contact">Contact</a>
        <a class="cta" href="#">Get Started</a>
      </nav>
    </header>

    <main>
      <section class="hero">
        <div class="left">
          <h1>Beautiful static website — ready to use</h1>
          <p class="lead">Fast, accessible and responsive. Perfect for portfolios, landing pages, and small projects. No build tools required — just save this file and open it in your browser.</p>
          <div class="cta-row">
            <button class="btn primary" onclick="ctaPrimary()">Download</button>
            <button class="btn" onclick="scrollToSection('features')">Explore features</button>
          </div>
          <div style="margin-top:18px;color:var(--muted);font-size:13px">Built with plain HTML, CSS and tiny JavaScript — easy to customize.</div>
        </div>
        <aside class="mock">
          <div class="row" style="justify-content:space-between;margin-bottom:14px">
            <div style="display:flex;gap:12px;align-items:center">
              <div class="avatar">MS</div>
              <div>
                <div style="font-weight:700">ModernSite — Template</div>
                <div style="font-size:13px;color:var(--muted)">v1.0 • static</div>
              </div>
            </div>
            <div style="font-size:12px;color:var(--muted)">Lightweight — 0 dependencies</div>
          </div>
          <div style="background:var(--glass);padding:12px;border-radius:10px;color:var(--muted);font-size:13px">Quick setup: save the file as <code>index.html</code> and open it.</div>
        </aside>
      </section>

      <section id="features" class="features">
        <div class="feature">
          <h3>Responsive</h3>
          <p>Layouts that adapt to phones, tablets and desktop automatically.</p>
        </div>
        <div class="feature">
          <h3>Accessible</h3>
          <p>Readable type, semantic markup, and keyboard-friendly controls.</p>
        </div>
        <div class="feature">
          <h3>Customizable</h3>
          <p>Change colors, fonts, and sections without touching build tools.</p>
        </div>
      </section>

      <section id="gallery" class="gallery">
        <div class="tile">Project A</div>
        <div class="tile">Project B</div>
        <div class="tile">Project C</div>
        <div class="tile">Project D</div>
      </section>

      <section id="contact" class="contact">
        <form onsubmit="event.preventDefault(); alert('Thanks — form submission simulated.')">
          <div style="display:flex;gap:12px;margin-bottom:12px">
            <div style="flex:1">
              <label for="name">Name</label>
              <input id="name" type="text" placeholder="Your name" required />
            </div>
            <div style="width:48%">
              <label for="email">Email</label>
              <input id="email" type="email" placeholder="you@example.com" required />
            </div>
          </div>
          <div style="margin-bottom:12px">
            <label for="message">Message</label>
            <textarea id="message" placeholder="Tell us about your project"></textarea>
          </div>
          <div style="display:flex;gap:8px;justify-content:flex-end">
            <button class="btn" type="reset">Reset</button>
            <button class="btn primary" type="submit">Send message</button>
          </div>
        </form>

        <div style="align-self:start;background:linear-gradient(180deg,#071122,#081628);padding:18px;border-radius:12px;border:1px solid rgba(255,255,255,0.03)">
          <h3 style="margin-top:0">Contact Info</h3>
          <p style="color:var(--muted)">Email: hello@modernsite.example</p>
          <p style="color:var(--muted)">Phone: +91 98765 43210</p>
          <p style="color:var(--muted);font-size:13px">Or just drop a message using the form.</p>
        </div>
      </section>

    </main>

    <footer>
      <div>© <span id="year"></span> ModernSite — All rights reserved</div>
      <div style="display:flex;gap:14px"><a href="#" style="color:var(--muted);text-decoration:none">Privacy</a><a href="#" style="color:var(--muted);text-decoration:none">Terms</a></div>
    </footer>
  </div>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
    function scrollToSection(id){
      const el = document.getElementById(id);
      if(el) el.scrollIntoView({behavior:'smooth'});
    }
    function ctaPrimary(){
      // Simulate download: create a blob of the current HTML and prompt download
      const html = '<!doctype html>\n' + document.documentElement.outerHTML;
      const blob = new Blob([html], {type: 'text/html'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url; a.download = 'modern-static-website.html';
      document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
    }
  </script>
</body>
</html>

